---
phase: 08-integration-tests-automation-for-dotnet-tool
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Portless.E2ETests/Portless.E2ETests.csproj
  - Portless.E2ETests/ToolInstallationTests.cs
  - Portless.E2ETests/CommandLineE2ETests.cs
  - Portless.E2ETests/CrossPlatformTests.cs
autonomous: false
requirements:
  - TEST-02
  - TEST-05

must_haves:
  truths:
    - Tool can be installed locally with dotnet tool install
    - Tool commands execute via Process.Start with real CLI invocation
    - Proxy routing works end-to-end with real HTTP requests
    - Tests run on Windows and Linux with platform-agnostic paths
    - Tool uninstall cleans up all artifacts
  artifacts:
    - path: "Portless.E2ETests/Portless.E2ETests.csproj"
      provides: "E2E test project for full tool installation testing"
      contains: "ProjectReference Portless.Cli, no WebApplicationFactory"
    - path: "Portless.E2ETests/ToolInstallationTests.cs"
      provides: "dotnet tool install/uninstall/verify tests"
      min_lines: 100
    - path: "Portless.E2ETests/CommandLineE2ETests.cs"
      provides: "Full CLI invocation tests with Process.Start"
      min_lines: 120
    - path: "Portless.E2ETests/CrossPlatformTests.cs"
      provides: "Platform-specific behavior validation"
      min_lines: 80
  key_links:
    - from: "Portless.E2ETests/ToolInstallationTests.cs"
      to: "dotnet tool CLI"
      via: "Process.Start with dotnet tool commands"
      pattern: "Process\\.Start\\(\"dotnet\".*\"tool\""
    - from: "Portless.E2ETests/CommandLineE2ETests.cs"
      to: "Portless proxy YARP"
      via: "HttpClient with custom Host header"
      pattern: "HttpClient.*SendAsync.*Host.*localhost"
    - from: "Portless.E2ETests/CrossPlatformTests.cs"
      to: "System.IO.Path"
      via: "Cross-platform path operations"
      pattern: "Path\\.(Combine|GetTempPath|DirectorySeparatorChar)"
---

<objective>
Create E2E test project for full tool installation, CLI invocation, and cross-platform validation.

Purpose: Validate Portless.NET works correctly when installed as a real dotnet tool with actual process execution (TEST-02, TEST-05).
Output: E2E test suite with tool installation tests, full workflow tests, and cross-platform path validation.
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-tests-automation-for-dotnet-tool/08-CONTEXT.md
@.planning/phases/08-integration-tests-automation-for-dotnet-tool/08-RESEARCH.md

# Reference existing test infrastructure
@Portless.Tests/ProxyRoutingTests.cs
@Portless.Tests/RoutePersistenceTests.cs
</context>

<tasks>

<task type="auto">
  <name>Create E2E test project structure</name>
  <files>Portless.E2ETests/Portless.E2ETests.csproj</files>
  <action>
    Create Portless.E2ETests project with:
    - TargetFramework: net10.0
    - Package references: xUnit 2.9.3, Microsoft.NET.Test.Sdk 17.14.1, coverlet.collector 6.0.4
    - Package reference: Spectre.Console.Testing for output validation
    - Project reference: Portless.Cli only (not Core or Proxy - testing via tool)
    - Using globals: XUnit, System.Diagnostics, System.Net.Http

    DO NOT add WebApplicationFactory (E2E tests use real HTTP, not TestServer)
    DO NOT add integration test project references (testing installed tool, not assemblies)

    Add project to Portless.slnx after Portless.IntegrationTests
  </action>
  <verify>dotnet build Portless.E2ETests/Portless.E2ETests.csproj succeeds</verify>
  <done>E2E test project compiles with minimal dependencies</done>
</task>

<task type="auto">
  <name>Create tool installation tests</name>
  <files>Portless.E2ETests/ToolInstallationTests.cs</files>
  <action>
    Create ToolInstallationTests class with:
    - Test class with IAsyncLifetime for setup/teardown
    - Private fields: string TestDirectory, string NupkgDirectory
    - InitializeAsync: Create directories, ensure tool uninstalled
    - DisposeAsync: Uninstall tool, cleanup directories (with retry logic, log warnings only)

    Test: ToolInstall_LocalInstall_Succeeds
      - Run: dotnet build Portless.slnx (capture exit code)
      - Assert build exit code is 0
      - Run: dotnet pack Portless.Cli/Portless.Cli.csproj -o {NupkgDirectory}
      - Assert pack exit code is 0
      - Run: dotnet tool install --add-source {NupkgDirectory} portless.dotnet
      - Assert install exit code is 0
      - Assert stdout contains "portless.dotnet"
      - Assert stdout contains "installed successfully"

    Test: ToolInstall_ToolCanBeInvoked
      - Precondition: Tool installed from previous test
      - Run: dotnet tool run portless --help
      - Assert exit code is 0
      - Assert stdout contains "proxy", "run", "list" commands

    Test: ToolUninstall_RemovesTool
      - Run: dotnet tool uninstall portless.dotnet
      - Assert exit code is 0
      - Run: dotnet tool run portless --help (expect failure)
      - Assert exit code is non-zero

    Use Process.Start for all dotnet commands
    RedirectStandardOutput and RedirectStandardError for validation
  </action>
  <verify>dotnet test --filter "FullyQualifiedName~ToolInstallationTests" passes all tests</verify>
  <done>Tool installs, executes, and uninstalls correctly</done>
</task>

<task type="auto">
  <name>Create full CLI workflow E2E tests</name>
  <files>Portless.E2ETests/CommandLineE2ETests.cs</files>
  <action>
    Create CommandLineE2ETests class with:
    - Test class with IAsyncLifetime
    - Private field: string TestDirectory = Path.Combine(Path.GetTempPath(), Path.GetRandomFileName())
    - Private field: Process? ProxyProcess (for cleanup)

    Test: ProxyStart_Run_List_Stop_CompletesSuccessfully
      - Step 1: Start proxy with dotnet run --project Portless.Proxy
        * Create ProcessStartInfo with RedirectStandardOutput, UseShellExecute=false
        * Start process, capture PID
        * Wait 2 seconds for startup
      - Step 2: Run test app with portless
        * Create test directory
        * Create dummy app that listens on PORT (use simple Python/Node script or use TestApi project)
        * Run: dotnet tool run portless testapi dotnet run --project TestApi/TestApi.csproj
        * Capture output
      - Step 3: List routes
        * Run: dotnet tool run portless list
        * Assert output contains testapi.localhost and assigned port
      - Step 4: Stop proxy
        * Run: dotnet tool run portless proxy stop
        * Verify proxy process terminated
      - Step 5: Verify route cleanup
        * Wait 30 seconds for RouteCleanupService
        * Run list again
        * Assert testapi route removed

    Test: RunCommand_InjectsPortVariable
      - Start proxy (same as above)
      - Create test app that writes PORT env var to stdout
      - Run with portless
      - Assert output contains "PORT=4xxx" (assigned port number)
      - Stop proxy

    Use TestApi/TestApi.csproj for dummy app if available
    Use 5-second delays between operations for process startup
    Always cleanup proxy process in DisposeAsync (Kill entireProcessTree:true)
  </action>
  <verify>dotnet test --filter "FullyQualifiedName~CommandLineE2ETests" passes all tests</verify>
  <done>Full CLI workflow (start/run/list/stop) works end-to-end</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Complete E2E test suite with tool installation and full workflow tests</what-built>
  <how-to-verify>
    1. Run all E2E tests: dotnet test Portless.E2ETests/Portless.E2ETests.csproj
    2. Verify tool installs correctly
    3. Verify proxy starts and stops cleanly
    4. Verify routes persist and cleanup
    5. Check no leaked processes in Task Manager
    6. Check no temp directories left in %TEMP%

    Expected results:
    - All tests pass (ToolInstallationTests + CommandLineE2ETests)
    - Tool installs, invokes, and uninstalls successfully
    - Proxy process starts, routes persist, cleanup works
    - No resource leaks after test completion
  </how-to-verify>
  <resume-signal>Type "approved" if tests pass and cleanup works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run dotnet test Portless.E2ETests/Portless.E2ETests.csproj — all tests pass
2. Verify tool can be installed and invoked manually
3. Check no dotnet.exe processes leaked
4. Verify temp directories cleaned up
5. Run tests twice — no conflicts from previous runs
</verification>

<success_criteria>
- E2E test project created and added to solution
- Tool installation tests pass (install, verify, uninstall)
- Full workflow tests pass (start/run/list/stop)
- PORT variable injection works correctly
- No resource leaks after test completion
- Tests can run multiple times without conflicts
</success_criteria>

<output>
After completion, create .planning/phases/08-integration-tests-automation-for-dotnet-tool/08-02-SUMMARY.md
</output>
