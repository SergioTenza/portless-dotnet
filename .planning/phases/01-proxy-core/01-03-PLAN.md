---
phase: 01-proxy-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Portless.Proxy/Program.cs
autonomous: true
gap_closure: true
requirements:
  - PROXY-02

must_haves:
  truths:
    - "RequestLoggingMiddleware executes for all proxied requests"
    - "Middleware pipeline order is correct (logging before proxy)"
    - "All proxied requests appear in console logs with method, host, path, status, duration"
  artifacts:
    - path: "Portless.Proxy/Program.cs"
      provides: "Correct middleware pipeline ordering"
      min_lines: 150
      contains: "UseMiddleware<RequestLoggingMiddleware>() before MapReverseProxy()"
  key_links:
    - from: "RequestLoggingMiddleware"
      to: "MapReverseProxy"
      via: "ASP.NET Core middleware pipeline"
      pattern: "UseMiddleware.*RequestLoggingMiddleware.*MapReverseProxy"

---

<objective>
Fix middleware ordering bug that prevents RequestLoggingMiddleware from executing for proxied requests.

Purpose: MapReverseProxy() is terminal middleware that terminates the pipeline. When placed before RequestLoggingMiddleware, the logging middleware never executes for proxied requests.

Output: Correct middleware pipeline order with logging happening before proxy forwarding.
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-proxy-core/01-01-PLAN.md
@.planning/phases/01-proxy-core/01-01-SUMMARY.md
@.planning/phases/01-proxy-core/01-02-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Gap Source
@.planning/phases/01-proxy-core/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder middleware pipeline in Program.cs</name>
  <files>Portless.Proxy/Program.cs</files>
  <action>
    Reorder the middleware pipeline in Portless.Proxy/Program.cs to fix the bug identified in VERIFICATION.md:

    **Current (WRONG):**
    - Line 56: `app.MapReverseProxy();` - handles requests, terminates pipeline
    - Line 141: `app.UseMiddleware&lt;RequestLoggingMiddleware&gt;();` - never reached for proxied requests

    **Required (CORRECT):**
    1. Move `app.UseMiddleware&lt;RequestLoggingMiddleware&gt;();` to line 56 (before MapReverseProxy)
    2. Move `app.MapReverseProxy();` to line 141 (after UseMiddleware, before app.Run)

    **Rationale from VERIFICATION.md Gap 1:**
    - MapReverseProxy() is terminal middleware - it handles requests and terminates the pipeline
    - Anything registered after it won't execute for proxied requests
    - RequestLoggingMiddleware must execute BEFORE MapReverseProxy to log proxied requests

    **Verification that fix works:**
    - After reorder, line 56 should have: `app.UseMiddleware&lt;RequestLoggingMiddleware&gt;();`
    - After reorder, line 141 should have: `app.MapReverseProxy();`
    - Middleware pipeline should be: UseMiddleware (logging) → MapReverseProxy (proxy) → API endpoint → Run

    Reference implementation pattern from ASP.NET Core: logging middleware should always be early in pipeline to capture all requests.
  </action>
  <verify>
    1. Build: `dotnet build Portless.slnx`
    2. Verify line 56 contains `UseMiddleware<RequestLoggingMiddleware>`
    3. Verify line 141 contains `MapReverseProxy`
    4. Verify no build errors or warnings
  </verify>
  <done>
    RequestLoggingMiddleware is registered before MapReverseProxy in the middleware pipeline, ensuring all proxied requests are logged with method, host, path, status code, and duration.
  </done>
</task>

</tasks>

<verification>
After fix, verify middleware ordering is correct:
- Read Program.cs and confirm UseMiddleware<RequestLoggingMiddleware>() appears before MapReverseProxy()
- The middleware pipeline should follow ASP.NET Core best practices: logging → proxy → API endpoints
</verification>

<success_criteria>
- [ ] Middleware pipeline reordered correctly
- [ ] UseMiddleware<RequestLoggingMiddleware>() executes before MapReverseProxy()
- [ ] Solution builds with 0 warnings, 0 errors
- [ ] All proxied requests will now be logged (verifiable at runtime)
</success_criteria>

<output>
After completion, create `.planning/phases/01-proxy-core/01-03-SUMMARY.md`
</output>
