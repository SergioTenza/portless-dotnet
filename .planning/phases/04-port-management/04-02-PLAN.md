---
phase: 04-port-management
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - Portless.Cli/Commands/RunCommand/RunCommand.cs
  - Portless.Core/Services/RouteCleanupService.cs
  - Portless.Core/Services/IPortAllocator.cs
  - Portless.Core/Services/PortAllocator.cs
autonomous: true
requirements:
  - PORT-03
  - PORT-04

must_haves:
  truths:
    - "RunCommand injects PORT variable via ProcessStartInfo.Environment"
    - "RunCommand passes PID to PortAllocator for tracking"
    - "RouteCleanupService releases ports when processes terminate"
    - "PORT injection works cross-platform (Windows, macOS, Linux)"
  artifacts:
    - path: "Portless.Cli/Commands/RunCommand/RunCommand.cs"
      provides: "Run command with cross-platform PORT injection"
      contains: "startInfo.Environment[\"PORT\"]"
    - path: "Portless.Core/Services/RouteCleanupService.cs"
      provides: "Automatic port release on process termination"
      contains: "_portAllocator.ReleasePortAsync"
    - path: "Portless.Core/Services/IPortAllocator.cs"
      provides: "Updated interface with PID parameter"
      exports: ["AssignFreePortAsync(int pid)", "ReleasePortAsync(int port)"]
  key_links:
    - from: "Portless.Cli/Commands/RunCommand/RunCommand.cs"
      to: "Portless.Core/Services/PortAllocator.cs"
      via: "Method call with PID"
      pattern: "AssignFreePortAsync\(process.Id\)"
    - from: "Portless.Core/Services/RouteCleanupService.cs"
      to: "Portless.Core/Services/PortPool.cs"
      via: "Port release on dead routes"
      pattern: "_portAllocator.ReleasePortAsync|_portPool.ReleaseByPid"
---

<objective>
Implement cross-platform PORT environment variable injection and automatic port release when processes terminate, completing the port management lifecycle.

Purpose: Current PORT injection uses Windows-specific batch file hacks that don't work on macOS/Linux. Ports are never released when processes die, causing port exhaustion over time. This plan implements cross-platform injection via ProcessStartInfo.Environment and integrates port release with RouteCleanupService.

Output: Cross-platform PORT injection, automatic port release on process termination, complete port lifecycle management.
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-port-management/04-CONTEXT.md
@.planning/phases/04-port-management/04-RESEARCH.md
@.planning/phases/04-port-management/04-01-SUMMARY.md
@Portless.Cli/Commands/RunCommand/RunCommand.cs
@Portless.Core/Services/RouteCleanupService.cs
@Portless.Core/Services/PortPool.cs
</context>

<tasks>

<task type="auto">
  <name>Update RunCommand with cross-platform PORT injection</name>
  <files>Portless.Cli/Commands/RunCommand/RunCommand.cs</files>
  <action>
Update RunCommand.cs ExecuteAsync method:

Step 2: Assign free port (line ~95)
- Change: var port = await _portAllocator.AssignFreePortAsync();
- To: var port = await _portAllocator.AssignFreePortAsync(process.Id);
- Note: process.Id not available yet, need to restructure

Restructure Steps 2-4 (current port allocation → process spawn order):
1. Assign port first without PID (or use dummy PID 0)
2. Create ProcessStartInfo with PORT injection
3. Start process to get PID
4. Re-assign port with real PID
5. Register route with proxy

Replace batch file PORT injection (lines 99-117) with ProcessStartInfo.Environment:
- Remove: tempBatchFile creation, batch content writing, batch file execution
- Replace with: ProcessStartInfo using commandArgs directly
- Set UseShellExecute = false (required for environment injection)
- Set RedirectStandardOutput = true, RedirectStandardError = true (for detached execution)
- Inject PORT: startInfo.Environment["PORT"] = port.ToString();
- Preserve existing environment: Don't create new Dictionary, modify existing one

Example from research (04-RESEARCH.md):
```csharp
var startInfo = new ProcessStartInfo
{
    FileName = commandArgs[0],
    Arguments = string.Join(" ", commandArgs.Skip(1)),
    UseShellExecute = false,  // Required for environment injection
    RedirectStandardOutput = true,
    RedirectStandardError = true
};
startInfo.Environment["PORT"] = port.ToString();
```

Handle PID timing issue:
- Option A: Assign port with PID=0, update after process starts
- Option B: Create ProcessStartInfo, get PID via Process.Start, then allocate port
- Recommendation: Option B - start process, get PID, allocate port with PID, then register route

Per user constraint (04-CONTEXT.md): Only inject PORT variable, no validation.
Per research (04-RESEARCH.md): ProcessStartInfo.Environment is cross-platform, preserves existing env vars.
  </action>
  <verify>
dotnet build Portless.Cli/Portless.Cli.csproj succeeds.
Manual test: Run command with PORT-dependent app, verify PORT variable is set.
  </verify>
  <done>
RunCommand uses ProcessStartInfo.Environment for PORT injection, works cross-platform, passes PID to PortAllocator.
  </done>
</task>

<task type="auto">
  <name>Integrate port release with RouteCleanupService</name>
  <files>Portless.Core/Services/RouteCleanupService.cs, Portless.Core/Services/IPortAllocator.cs</files>
  <action>
Update RouteCleanupService.cs:

Add dependency injection:
- Add IPortAllocator _portAllocator to constructor parameters
- Assign to field in constructor

Update ExecuteAsync method (after line 42, route cleanup):
- After determining dead routes, extract ports: var deadPorts = routes.Where(r => !aliveRoutes.Contains(r)).Select(r => r.Port).ToArray();
- Release ports: foreach (var port in deadPorts) { await _portAllocator.ReleasePortAsync(port); }
- Log port releases: _logger.LogInformation("Released {Count} ports from dead processes", deadPorts.Length);

Updated flow:
1. Load routes
2. Detect dead processes
3. Remove dead routes from storage (existing)
4. Release ports from dead processes (new)
5. Trigger YARP reload (existing placeholder)

Update IPortAllocator.cs:
- Ensure ReleasePortAsync signature: Task ReleasePortAsync(int port);
- Add XML documentation: "Releases a port back to the pool for reuse"

Per research (04-RESEARCH.md): Integrate with existing RouteCleanupService lifecycle, use Process.HasExited detection.
Per plan 04-01: PortPool.ReleaseByPid removes all port mappings for given PID.
  </action>
  <verify>
dotnet build Portless.Core/Portless.Core.csproj succeeds.
Review RouteCleanupService.ExecuteAsync logic flow confirms port release happens after route cleanup.
  </verify>
  <done>
RouteCleanupService injects IPortAllocator, releases ports when processes die, ports returned to pool for reuse.
  </done>
</task>

</tasks>

<verification>
Cross-platform verification:
- ProcessStartInfo.Environment pattern works on Windows, macOS, Linux (per research)
- No OS-specific code (batch files, shell scripts) in RunCommand
- UseShellExecute = false for environment injection

Integration verification:
- RunCommand passes PID to PortAllocator.AssignFreePortAsync
- RouteCleanupService calls PortAllocator.ReleasePortAsync for dead processes
- PortPool tracks allocations, releases by PID or port

Lifecycle verification:
- Port allocated with PID → Process starts → Process dies → Cleanup service detects → Port released
- Port returned to pool available for reuse
</verification>

<success_criteria>
- RunCommand injects PORT via ProcessStartInfo.Environment (cross-platform)
- RunCommand passes process PID to AssignFreePortAsync
- RouteCleanupService releases ports when processes terminate
- IPortAllocator.ReleasePortAsync signature updated
- No Windows-specific batch file code in RunCommand
- Solution builds without errors
- Port lifecycle complete: allocate → inject → track → release
</success_criteria>

<output>
After completion, create .planning/phases/04-port-management/04-02-SUMMARY.md
</output>
