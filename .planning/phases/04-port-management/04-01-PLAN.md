---
phase: 04-port-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Portless.Core/Services/IPortPool.cs
  - Portless.Core/Services/PortPool.cs
  - Portless.Core/Services/PortAllocator.cs
  - Portless.Cli/Services/IPortAllocator.cs
  - Portless.Cli/Services/PortAllocator.cs
autonomous: true
requirements:
  - PORT-01
  - PORT-02

must_haves:
  truths:
    - "PortAllocator assigns unique ports without conflicts"
    - "PortPool tracks allocated ports with PID mappings"
    - "System detects port availability via TCP binding"
    - "Port allocation fails gracefully when range exhausted"
  artifacts:
    - path: "Portless.Core/Services/IPortPool.cs"
      provides: "Port pool interface with allocation tracking"
      exports: ["Allocate", "ReleaseByPid", "IsPortAllocated"]
    - path: "Portless.Core/Services/PortPool.cs"
      provides: "Port pool implementation with PID tracking"
      contains: "Dictionary<int, int> _portToPid"
    - path: "Portless.Core/Services/PortAllocator.cs"
      provides: "Enhanced port allocator with pooling integration"
      exports: ["AssignFreePortAsync", "ReleasePortAsync"]
  key_links:
    - from: "Portless.Core/Services/PortAllocator.cs"
      to: "Portless.Core/Services/PortPool.cs"
      via: "Composition pattern"
      pattern: "_portPool.Allocate|_portPool.IsPortAllocated"
    - from: "Portless.Cli/Services/PortAllocator.cs"
      to: "Portless.Core/Services/PortAllocator.cs"
      via: "Delegation pattern"
      pattern: "Core.PortAllocator"
---

<objective>
Implement port pooling and lifecycle management to prevent port conflicts and enable port reuse when processes terminate.

Purpose: Current port allocation is ephemeral - ports are assigned but never tracked or released, leading to potential conflicts and inefficient port usage. Port pooling with PID tracking enables proper lifecycle management and integration with RouteCleanupService.

Output: PortPool service for tracking allocations, enhanced PortAllocator with pooling integration, cross-platform port detection.
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-port-management/04-CONTEXT.md
@.planning/phases/04-port-management/04-RESEARCH.md
@Portless.Cli/Services/PortAllocator.cs
@Portless.Core/Services/RouteCleanupService.cs
@Portless.Core/Models/RouteInfo.cs
</context>

<tasks>

<task type="auto">
  <name>Create IPortPool interface and PortPool implementation</name>
  <files>Portless.Core/Services/IPortPool.cs, Portless.Core/Services/PortPool.cs</files>
  <action>
Create IPortPool.cs in Portless.Core/Services/:
- Define interface with methods: Allocate(int port, int pid), ReleaseByPid(int pid), ReleaseByPort(int port), IsPortAllocated(int port)
- Add XML documentation comments explaining PID tracking rationale

Create PortPool.cs implementing IPortPool:
- Use Dictionary<int, int> _portToPid to track port → PID mappings
- Implement Allocate(int port, int pid): adds mapping, throws if port already allocated
- Implement ReleaseByPid(int pid): removes all ports mapped to given PID (supports multi-port scenarios)
- Implement ReleaseByPort(int port): removes single port mapping
- Implement IsPortAllocated(int port): checks if port exists in mapping
- Add thread-safety using lock(_portToPid) for all public methods (RouteCleanupService runs on background thread)
- Add logging using ILogger<PortPool> for allocation/release events

Reference existing RouteCleanupService pattern for PID tracking validation.
Per research: Use Dictionary with lock for thread-safety, integrate with existing RouteCleanupService lifecycle.
  </action>
  <verify>
dotnet build Portless.Core/Portless.Core.csproj succeeds with no warnings.
  </verify>
  <done>
IPortPool and PortPool compile successfully, all interface methods implemented, thread-safe with lock statements.
  </done>
</task>

<task type="auto">
  <name>Move and enhance PortAllocator in Portless.Core</name>
  <files>Portless.Core/Services/PortAllocator.cs, Portless.Cli/Services/IPortAllocator.cs, Portless.Cli/Services/PortAllocator.cs</files>
  <action>
Create new PortAllocator.cs in Portless.Core/Services/:
- Copy existing Portless.Cli.Services.PortAllocator implementation
- Add constructor injection of IPortPool and ILogger<PortAllocator>
- Enhance AssignFreePortAsync(): check _portPool.IsPortAllocated(port) before TCP binding check
- Enhance AssignFreePortAsync(): call _portPool.Allocate(port, pid) after successful port detection (pid parameter needed)
- Change signature: async Task<int> AssignFreePortAsync(int pid)
- Implement ReleasePortAsync(int port): call _portPool.ReleaseByPort(port), log release event
- Keep existing IsPortFreeAsync(port) with TcpListener binding (per research: industry standard)
- Keep existing retry logic with 50 attempt limit

Update Portless.Cli/Services/IPortAllocator.cs:
- Update AssignFreePortAsync signature to include int pid parameter
- Keep ReleasePortAsync signature

Update Portless.Cli/Services/PortAllocator.cs:
- Change from implementation to wrapper/delegation pattern
- Constructor injects Portless.Core.Services.PortAllocator (not implementation)
- Delegate all calls to Core.PortAllocator instance
- This resolves duplication and provides single source of truth

Per user constraint (04-CONTEXT.md): No validation of PORT usage - only inject and track.
Per research (04-RESEARCH.md): TcpListener.Start()/Stop() is industry standard for port detection.
  </action>
  <verify>
dotnet build Portless.slnx succeeds with no compilation errors.
All references to old Portless.Cli.Services.PortAllocator resolve to new Core version.
  </verify>
  <done>
PortAllocator moved to Portless.Core, accepts PID parameter, integrates with PortPool for allocation tracking, CLI version delegates to Core version.
  </done>
</task>

</tasks>

<verification>
Build verification:
- dotnet build Portless.slnx compiles entire solution without errors
- All projects reference correct PortAllocator (Core, not CLI)

Integration verification:
- PortPool tracks allocations with PID mappings
- PortAllocator checks pool before TCP binding
- Thread-safe operations with lock statements
</verification>

<success_criteria>
- PortPool service created with PID tracking and thread-safety
- PortAllocator moved to Portless.Core and enhanced with pooling
- PortAllocator.AssignFreePortAsync(int pid) accepts PID parameter
- PortAllocator integrates with PortPool for allocation tracking
- CLI PortAllocator delegates to Core PortAllocator (no duplication)
- Solution builds without compilation errors
</success_criteria>

<output>
After completion, create .planning/phases/04-port-management/04-01-SUMMARY.md
</output>
