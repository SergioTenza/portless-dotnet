---
phase: 06-dotnet-integration
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - scripts/install.sh
  - scripts/install.ps1
  - docs/tutorials/01-migration.md
  - docs/tutorials/02-from-scratch.md
  - docs/tutorials/03-microservices.md
  - docs/tutorials/04-e2e-testing.md
  - docs/integration/launch-settings.md
  - docs/integration/appsettings.md
  - docs/integration/kestrel-configuration.md
  - docs/README.md
autonomous: true
requirements:
  - DOTNET-02
  - DOTNET-03
user_setup: []

must_haves:
  truths:
    - "User can install Portless.NET using installation scripts on Windows/Linux/macOS"
    - "User can follow migration tutorial to convert existing project to use Portless"
    - "User can follow from-scratch tutorial to create new project with Portless"
    - "User can follow microservices tutorial to run multiple services with Portless"
    - "User can follow E2E testing tutorial to test with stable URLs"
    - "Integration guides explain launchSettings.json configuration"
    - "Integration guides explain appsettings.json configuration"
    - "Integration guides explain Kestrel configuration options"
    - "Documentation is discoverable via README and cross-references"
  artifacts:
    - path: "scripts/install.sh"
      provides: "Cross-platform installation script for Unix-like systems"
      contains: ["dotnet tool install", "PATH", "portless"]
      min_lines: 30
    - path: "scripts/install.ps1"
      provides: "PowerShell installation script for Windows"
      contains: ["dotnet tool install", "PATH", "portless"]
      min_lines: 30
    - path: "docs/tutorials/01-migration.md"
      provides: "Step-by-step guide for migrating existing projects"
      min_lines: 80
    - path: "docs/tutorials/02-from-scratch.md"
      provides: "Step-by-step guide for creating new projects"
      min_lines: 80
    - path: "docs/tutorials/03-microservices.md"
      provides: "Microservices scenario with multiple services"
      min_lines: 100
    - path: "docs/tutorials/04-e2e-testing.md"
      provides: "E2E testing guide with stable URLs"
      min_lines: 80
    - path: "docs/integration/launch-settings.md"
      provides: "launchSettings.json integration reference"
      min_lines: 60
    - path: "docs/integration/appsettings.md"
      provides: "appsettings.json integration reference"
      min_lines: 60
    - path: "docs/integration/kestrel-configuration.md"
      provides: "Kestrel configuration options reference"
      min_lines: 60
    - path: "docs/README.md"
      provides: "Documentation index and navigation"
      min_lines: 40
  key_links:
    - from: "scripts/install.sh"
      to: "~/.dotnet/tools/portless"
      via: "dotnet tool install --global"
      pattern: "dotnet tool install"
    - from: "scripts/install.ps1"
      to: "user PATH environment variable"
      via: "[Environment]::SetEnvironmentVariable"
      pattern: "SetEnvironmentVariable"
    - from: "tutorials"
      to: "integration guides"
      via: "Cross-references in README and tutorials"
      pattern: "\\[.*integration.*\\]"
    - from: "documentation"
      to: "example projects"
      via: "References to Examples/ directory"
      pattern: "Examples/"
---

<objective>
Create comprehensive documentation tutorials and cross-platform installation automation for Portless.NET

Purpose: Enable users to install Portless.NET automatically via scripts (Windows/Linux/macOS), learn how to integrate it into existing or new projects through detailed tutorials, and reference integration guides for configuration options.

Output: Cross-platform installation scripts, 4 comprehensive tutorials, 3 integration guides, and a documentation README providing clear navigation and discoverability.
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dotnet-integration/06-CONTEXT.md
@.planning/phases/06-dotnet-integration/06-RESEARCH.md
@.planning/REQUIREMENTS.md

# Example projects for documentation references
@.planning/phases/06-dotnet-integration/06-02-PLAN.md

# Current project structure
@Portless.Cli/Portless.Cli.csproj
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Create cross-platform installation scripts</name>
  <files>
    scripts/install.sh
    scripts/install.ps1
  </files>
  <action>
    Create installation scripts following Microsoft's dotnet-install pattern:

    **scripts/install.sh** (Unix-like systems):
    ```bash
    #!/bin/bash
    set -e

    echo "Installing Portless.NET..."
    echo ""

    # Check if .NET SDK is installed
    if ! command -v dotnet &> /dev/null; then
        echo "Error: .NET SDK is not installed."
        echo "Please install .NET SDK from https://dotnet.microsoft.com/download"
        exit 1
    fi

    # Install from NuGet.org (or custom source)
    echo "Installing Portless.NET.Tool from NuGet..."
    dotnet tool install --global Portless.NET.Tool --version 1.0.0

    # Add to PATH (if not already)
    TOOL_PATH="$HOME/.dotnet/tools"
    if [[ ":$PATH:" != *":$TOOL_PATH:"* ]]; then
        echo ""
        echo "Adding $TOOL_PATH to PATH..."

        # Detect shell and update appropriate config file
        if [ -n "$ZSH_VERSION" ]; then
            CONFIG_FILE="$HOME/.zshrc"
        elif [ -n "$BASH_VERSION" ]; then
            CONFIG_FILE="$HOME/.bashrc"
        else
            CONFIG_FILE="$HOME/.profile"
        fi

        echo "export PATH=\"\$PATH:\$HOME/.dotnet/tools\"" >> "$CONFIG_FILE"
        export PATH="$PATH:$HOME/.dotnet/tools"

        echo "Added to $CONFIG_FILE"
        echo "Please restart your terminal or run: source $CONFIG_FILE"
    fi

    echo ""
    # Verify installation
    if command -v portless &> /dev/null; then
        echo "✓ Portless installed successfully!"
        echo ""
        portless --version 2>/dev/null || echo "Version: 1.0.0"
        echo ""
        echo "Getting started:"
        echo "  1. Start the proxy: portless proxy start"
        echo "  2. Run your app:    portless myapp dotnet run"
        echo "  3. Access via URL:  http://myapp.localhost"
    else
        echo "⚠ Installation completed, but 'portless' command not found in PATH."
        echo "Please restart your terminal or run:"
        echo "  export PATH=\"\$PATH:\$HOME/.dotnet/tools\""
        exit 1
    fi
    ```

    **scripts/install.ps1** (Windows PowerShell):
    ```powershell
    #Requires -Version 5.1
    $ErrorActionPreference = "Stop"

    Write-Host "Installing Portless.NET..." -ForegroundColor Green
    Write-Host ""

    # Check if .NET SDK is installed
    if (-not (Get-Command dotnet -ErrorAction SilentlyContinue)) {
        Write-Host "Error: .NET SDK is not installed." -ForegroundColor Red
        Write-Host "Please install .NET SDK from https://dotnet.microsoft.com/download"
        exit 1
    }

    # Install from NuGet.org
    Write-Host "Installing Portless.NET.Tool from NuGet..." -ForegroundColor Cyan
    dotnet tool install --global Portless.NET.Tool --version 1.0.0

    # Add to PATH (persistent)
    $toolPath = "$env:USERPROFILE\.dotnet\tools"
    $pathEnv = [Environment]::GetEnvironmentVariable("Path", "User")

    if ($pathEnv -notlike "*$toolPath*") {
        Write-Host ""
        Write-Host "Adding $toolPath to user PATH..." -ForegroundColor Cyan

        [Environment]::SetEnvironmentVariable("Path", "$pathEnv;$toolPath", "User")
        $env:Path = "$env:Path;$toolPath"

        Write-Host "Added to user PATH (persistent across restarts)"
        Write-Host "Please restart your terminal for PATH changes to take effect"
    }

    Write-Host ""
    # Verify installation
    if (Get-Command portless -ErrorAction SilentlyContinue) {
        Write-Host "✓ Portless installed successfully!" -ForegroundColor Green
        Write-Host ""

        try {
            portless --version
        } catch {
            Write-Host "Version: 1.0.0"
        }

        Write-Host ""
        Write-Host "Getting started:" -ForegroundColor Cyan
        Write-Host "  1. Start the proxy: portless proxy start"
        Write-Host "  2. Run your app:    portless myapp dotnet run"
        Write-Host "  3. Access via URL:  http://myapp.localhost"
    } else {
        Write-Host "⚠ Installation completed, but 'portless' command not found in PATH." -ForegroundColor Yellow
        Write-Host "Please restart your terminal for PATH changes to take effect"
        exit 1
    }
    ```

    Make the bash script executable and add execution to both scripts. The scripts follow the standard pattern of:
    1. Checking prerequisites (.NET SDK)
    2. Installing the tool via dotnet tool install
    3. Updating PATH persistently
    4. Verifying installation
    5. Providing next steps
  </action>
  <verify>
    Run: `ls -la scripts/` and verify:
    1. scripts/install.sh exists with executable permissions (run `chmod +x scripts/install.sh`)
    2. scripts/install.ps1 exists
    3. Both files contain dotnet tool install command
    4. Both files contain PATH configuration logic
  </verify>
  <done>
    scripts/install.sh exists with proper shebang and executable permissions, scripts/install.ps1 exists with #Requires header, both scripts install Portless.NET.Tool and configure PATH, and both scripts verify installation and provide next steps
  </done>
</task>

<task type="auto">
  <name>Create tutorial 1: Migrating existing projects</name>
  <files>docs/tutorials/01-migration.md</files>
  <action>
    Create a comprehensive migration tutorial (docs/tutorials/01-migration.md):

    ```markdown
    # Tutorial 1: Migrating an Existing Project to Portless

    This tutorial guides you through migrating an existing ASP.NET Core project to use Portless.NET for stable .localhost URLs during development.

    ## Prerequisites

    - .NET 10 SDK installed
    - Portless.NET installed: `dotnet tool install --global Portless.NET.Tool`
    - An existing ASP.NET Core project

    ## Overview

    Migration involves three steps:
    1. Configure your application to read the PORT environment variable
    2. Update launchSettings.json for dynamic port assignment
    3. Run your application with Portless

    ## Step 1: Configure PORT Variable Reading

    Open your project's `Program.cs` and add PORT integration before `var app = builder.Build();`:

    \`\`\`csharp
    var builder = WebApplication.CreateBuilder(args);

    // Portless integration: Read PORT from environment
    var port = Environment.GetEnvironmentVariable("PORT");
    if (!string.IsNullOrEmpty(port))
    {
        builder.WebHost.UseUrls($"http://*:{port}");
    }

    // ... existing service configuration

    var app = builder.Build();
    // ... existing middleware configuration
    \`\`\`

    **Why this works:**
    - Portless injects the `PORT` environment variable when starting your application
    - `UseUrls()` configures Kestrel to bind to the specified port
    - The `*` wildcard binds to all network interfaces (localhost, 127.0.0.1, etc.)

    **Alternative: Using Kestrel Configuration**
    If you prefer configuration over code, see [Kestrel Configuration](../integration/kestrel-configuration.md).

    ## Step 2: Update launchSettings.json

    Open `Properties/launchSettings.json` and add a "Portless" profile:

    \`\`\`json
    {
      "$schema": "http://json.schemastore.org/launchsettings.json",
      "profiles": {
        "Portless": {
          "commandName": "Project",
          "launchBrowser": true,
          "applicationUrl": "http://localhost:0",
          "environmentVariables": {
            "ASPNETCORE_ENVIRONMENT": "Development"
          }
        },
        "https": {
          "commandName": "Project",
          "launchBrowser": true,
          "applicationUrl": "http://localhost:5000;https://localhost:5001",
          "environmentVariables": {
            "ASPNETCORE_ENVIRONMENT": "Development"
          }
        }
      }
    }
    \`\`\`

    **Key changes:**
    - Added "Portless" profile with `applicationUrl: "http://localhost:0"`
    - `localhost:0` allows dynamic port assignment (Portless assigns the actual port)
    - Preserved existing profiles for non-Portless development

    **Note:** Keep your existing profiles! You can switch between Portless and direct execution in Visual Studio or VS Code.

    ## Step 3: Run with Portless

    Start the Portless proxy:
    \`\`\`bash
    portless proxy start
    \`\`\`

    Run your application with Portless:
    \`\`\`bash
    # Navigate to your project directory
    cd path/to/your/project

    # Run with Portless (replace 'myapp' with your preferred hostname)
    portless myapp dotnet run
    \`\`\`

    Access your application at: `http://myapp.localhost`

    ## Verification

    1. **Check proxy is running:**
       \`\`\`bash
       portless proxy status
       \`\`\`
       Expected: "Proxy is running on port 1355"

    2. **Check your route is registered:**
       \`\`\`bash
       portless list
       \`\`\`
       Expected output:
       \`\`\`
       ┌──────────┬───────┬─────────┬───────┐
       │ Hostname │ Port  │ Process │ PID   │
       ├──────────┼───────┼─────────┼───────┤
       │ myapp    │ 4001  │ dotnet  │ 12345 │
       └──────────┴───────┴─────────┴───────┘
       \`\`\`

    3. **Test the URL:**
       \`\`\`bash
       curl http://myapp.localhost/
       # Or open in browser: http://myapp.localhost/
       \`\`\`

    ## Troubleshooting

    ### Application doesn't use the assigned port

    **Symptom:** Application runs on default port (5000) instead of Portless-assigned port (4001)

    **Solution:** Ensure `UseUrls()` is called BEFORE `builder.Build()`. The order matters:

    ✅ Correct:
    \`\`\`csharp
    var port = Environment.GetEnvironmentVariable("PORT");
    if (!string.IsNullOrEmpty(port))
    {
        builder.WebHost.UseUrls($"http://*:{port}");
    }
    var app = builder.Build();
    \`\`\`

    ❌ Incorrect:
    \`\`\`csharp
    var app = builder.Build();
    var port = Environment.GetEnvironmentVariable("PORT");
    // UseUrls after Build() has no effect
    \`\`\`

    ### Portless proxy not running

    **Symptom:** `portless proxy status` returns "Proxy is not running"

    **Solution:** Start the proxy:
    \`\`\`bash
    portless proxy start
    \`\`\`

    ### hostname.localhost doesn't resolve

    **Symptom:** Browser shows "This site can't be reached" or DNS error

    **Solution:** Ensure Portless proxy is running on port 1355 and your route is registered. The `.localhost` TLD is reserved for local development and should resolve automatically on modern OSs.

    If you still have issues, try:
    \`\`\`bash
    # Check if proxy is listening
    curl http://localhost:1355/api/v1/health

    # Check routes
    portless list
    \`\`\`

    ### Multiple applications on same port

    **Symptom:** Two different applications show the same port in `portless list`

    **Solution:** Portless automatically assigns unique ports. If you see conflicts, ensure each application is started with a unique hostname:
    \`\`\`bash
    # Terminal 1
    portless app1 dotnet run

    # Terminal 2
    portless app2 dotnet run
    \`\`\`

    ## Advanced: appsettings.json Integration

    If you prefer configuration over code, you can integrate Portless via `appsettings.json`:

    \`\`\`json
    {
      "Kestrel": {
        "Endpoints": {
          "Http": {
            "Url": "http://0.0.0.0:${PORT}",
            "Protocols": "Http1AndHttp2"
          }
        }
      }
    }
    \`\`\`

    See [appsettings.json Integration](../integration/appsettings.md) for details.

    ## Next Steps

    - [Tutorial 2: Creating a New Project with Portless](02-from-scratch.md)
    - [Tutorial 3: Microservices Scenario](03-microservices.md)
    - [launchSettings.json Reference](../integration/launch-settings.md)

    ## Example Project

    See the `Examples/WebApi` directory in the Portless.NET repository for a complete working example.
    ```

    The tutorial should be comprehensive, code-first, and include troubleshooting for common issues.
  </action>
  <verify>
    Run: `wc -l docs/tutorials/01-migration.md` and verify:
    1. File has at least 80 lines
    2. Contains code examples for Program.cs modification
    3. Contains launchSettings.json example
    4. Includes troubleshooting section
  </verify>
  <done>
    docs/tutorials/01-migration.md exists with at least 80 lines, includes step-by-step migration instructions with code examples, explains PORT integration, provides troubleshooting for common issues, and cross-references other documentation
  </done>
</task>

<task type="auto">
  <name>Create tutorials 2-4 and integration guides</name>
  <files>
    docs/tutorials/02-from-scratch.md
    docs/tutorials/03-microservices.md
    docs/tutorials/04-e2e-testing.md
    docs/integration/launch-settings.md
    docs/integration/appsettings.md
    docs/integration/kestrel-configuration.md
    docs/README.md
  </files>
  <action>
    Create the remaining tutorials and integration guides:

    **docs/tutorials/02-from-scratch.md** - New project creation tutorial:
    - Creating a new ASP.NET Core project
    - Configuring PORT integration from the start
    - Setting up launchSettings.json
    - Running with Portless
    - Best practices for new projects

    **docs/tutorials/03-microservices.md** - Microservices scenario:
    - Creating multiple services (API, Frontend, Worker)
    - Running all services with Portless
    - Service-to-service communication via .localhost URLs
    - Managing multiple services
    - Example architecture with 3+ services

    **docs/tutorials/04-e2e-testing.md** - E2E testing with stable URLs:
    - Why stable URLs matter for E2E tests
    - Configuring test fixtures with Portless
    - Playwright/Selenium examples with .localhost URLs
    - CI/CD integration
    - Parallel test execution

    **docs/integration/launch-settings.md** - launchSettings.json reference:
    - Schema and structure explanation
    - Portless-specific configuration
    - Profile examples (Portless, Direct, HTTPS)
    - Common patterns and pitfalls

    **docs/integration/appsettings.md** - appsettings.json reference:
    - Configuration-based PORT integration
    - Kestrel endpoints configuration
    - Environment-specific settings
    - Configuration builder setup

    **docs/integration/kestrel-configuration.md** - Kestrel configuration reference:
    - Program.cs code-based configuration
    - UseUrls vs ConfigureKestrel
    - HTTPS configuration
    - Advanced Kestrel options

    **docs/README.md** - Documentation index:
    ```markdown
    # Portless.NET Documentation

    Welcome to the Portless.NET documentation. Portless.NET provides stable .localhost URLs for local .NET development.

    ## Quick Start

    1. [Install Portless.NET](../../scripts/install.sh) (Linux/macOS) or [install.ps1](../../scripts/install.ps1) (Windows)
    2. Start the proxy: `portless proxy start`
    3. Run your app: `portless myapp dotnet run`
    4. Access via: `http://myapp.localhost`

    ## Tutorials

    Step-by-step guides for common scenarios:

    - [Tutorial 1: Migrating an Existing Project](tutorials/01-migration.md)
      - Add PORT integration to existing projects
      - Update launchSettings.json
      - Common migration issues

    - [Tutorial 2: Creating a New Project](tutorials/02-from-scratch.md)
      - Start with Portless from day one
      - Configure new projects correctly
      - Best practices

    - [Tutorial 3: Microservices Scenario](tutorials/03-microservices.md)
      - Run multiple services with Portless
      - Service-to-service communication
      - Managing complex architectures

    - [Tutorial 4: E2E Testing](tutorials/04-e2e-testing.md)
      - Stable URLs for reliable tests
      - Test automation integration
      - CI/CD setup

    ## Integration Guides

    Reference documentation for specific configuration options:

    - [launchSettings.json Integration](integration/launch-settings.md)
      - Profile configuration
      - Dynamic port assignment
      - Environment variables

    - [appsettings.json Integration](integration/appsettings.md)
      - Configuration-based setup
      - Kestrel endpoints
      - Environment-specific settings

    - [Kestrel Configuration](integration/kestrel-configuration.md)
      - Code-based configuration
      - UseUrls vs ConfigureKestrel
      - HTTPS setup

    ## Examples

    See the [Examples](../../Examples/) directory for complete working examples:
    - **WebApi** - ASP.NET Core Web API
    - **BlazorApp** - Blazor Web App
    - **WorkerService** - Background service
    - **ConsoleApp** - Console application

    ## CLI Reference

    Common commands:
    - `portless proxy start` - Start the proxy
    - `portless proxy stop` - Stop the proxy
    - `portless proxy status` - Check proxy status
    - `portless list` - List active routes
    - `portless <hostname> <command>` - Run app with URL

    See `portless --help` for full command reference.

    ## Troubleshooting

    **Proxy not running?**
    - Run: `portless proxy status`
    - Start: `portless proxy start`

    **App not using assigned port?**
    - Check Program.cs: Ensure `UseUrls()` is called before `Build()`
    - See: [Integration Guides](#integration-guides)

    **hostname.localhost not resolving?**
    - Ensure proxy is running: `portless proxy status`
    - Check route exists: `portless list`
    - See: [Tutorial 1 Troubleshooting](tutorials/01-migration.md#troubleshooting)

    ## Contributing

    Found an issue or have a suggestion? Please open an issue on GitHub.

    ## License

    Portless.NET is licensed under the MIT License.
    ```

    Each document should be comprehensive, code-first, and include practical examples. Use consistent formatting, cross-references, and ensure all documentation follows the same style.
  </action>
  <verify>
    Run: `find docs -name "*.md" -exec wc -l {} +` and verify:
    1. All 7 markdown files exist (tutorials: 4, integration: 3, README: 1)
    2. Each tutorial has at least 80 lines
    3. Each integration guide has at least 60 lines
    4. README.md has at least 40 lines
  </verify>
  <done>
    All 7 documentation files exist with minimum line counts, tutorials cover migration, new projects, microservices, and E2E testing scenarios, integration guides cover launchSettings.json, appsettings.json, and Kestrel configuration, docs/README.md provides navigation index, and all documents include cross-references and code examples
  </done>
</task>

</tasks>

<verification>
## Phase Verification Steps

After completing all tasks, verify the following:

1. **Installation Scripts**: Check that both scripts exist and are executable:
   - `ls -la scripts/` should show install.sh and install.ps1
   - `scripts/install.sh` should be executable (chmod +x)
   - Both scripts contain `dotnet tool install` and PATH configuration

2. **Tutorial Files**: Verify all 4 tutorial files exist:
   - docs/tutorials/01-migration.md
   - docs/tutorials/02-from-scratch.md
   - docs/tutorials/03-microservices.md
   - docs/tutorials/04-e2e-testing.md

3. **Integration Guides**: Verify all 3 integration guides exist:
   - docs/integration/launch-settings.md
   - docs/integration/appsettings.md
   - docs/integration/kestrel-configuration.md

4. **Documentation README**: Verify docs/README.md exists and provides navigation to all tutorials and guides.

5. **Content Quality**: Spot-check key documents:
   - Tutorial 01 should include Program.cs code examples
   - Tutorial 03 should include multi-service architecture
   - Integration guides should include configuration examples
   - README should include Quick Start section

6. **Cross-References**: Verify documents reference each other (e.g., tutorials reference integration guides, README references tutorials).

7. **Functional Testing** (optional manual verification):
   - Review install.sh script for proper syntax
   - Review install.ps1 script for proper PowerShell syntax
   - Check that all code examples are properly formatted in markdown

If manual testing is performed, verify the installation scripts have no syntax errors and the documentation renders correctly in a markdown viewer.
</verification>

<success_criteria>
1. ✅ scripts/install.sh exists with executable permissions and installs Portless
2. ✅ scripts/install.ps1 exists and installs Portless on Windows
3. ✅ All 4 tutorials exist with comprehensive content (min 80 lines each)
4. ✅ All 3 integration guides exist with reference content (min 60 lines each)
5. ✅ docs/README.md provides navigation and Quick Start
6. ✅ All documents include code examples and cross-references
7. ✅ Documentation covers all user scenarios from CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-dotnet-integration/06-03-SUMMARY.md` with:
- List of all documentation files created
- Installation script capabilities (PATH config, verification, error handling)
- Tutorial coverage summary
- Integration guide topics covered
- Any deviations from planned content
</output>
