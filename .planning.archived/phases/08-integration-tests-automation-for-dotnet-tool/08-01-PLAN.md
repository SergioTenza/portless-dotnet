---
phase: 08-integration-tests-automation-for-dotnet-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Portless.IntegrationTests/Portless.IntegrationTests.csproj
  - Portless.IntegrationTests/CliCommandTests.cs
  - Portless.IntegrationTests/ProxyProcessTests.cs
  - Portless.IntegrationTests/PortAllocatorTests.cs
autonomous: true
requirements:
  - TEST-01
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - CLI commands can be tested in-process using CommandApp with DI
    - Proxy process lifecycle (start/stop) can be tested with PID tracking
    - Port allocation detects free ports in 4000-4999 range without conflicts
    - Route persistence works with file locking and cleanup
    - Tests run independently with unique temp directories per test
  artifacts:
    - path: "Portless.IntegrationTests/Portless.IntegrationTests.csproj"
      provides: "Integration test project with xUnit and Spectre.Console testing support"
      contains: "ProjectReference Portless.Cli, Portless.Core, Portless.Proxy"
    - path: "Portless.IntegrationTests/CliCommandTests.cs"
      provides: "CLI command integration tests using CommandApp"
      min_lines: 80
    - path: "Portless.IntegrationTests/ProxyProcessTests.cs"
      provides: "Proxy lifecycle management tests"
      min_lines: 100
    - path: "Portless.IntegrationTests/PortAllocatorTests.cs"
      provides: "Port allocation and detection tests"
      min_lines: 60
  key_links:
    - from: "Portless.IntegrationTests/CliCommandTests.cs"
      to: "Portless.Cli.Commands"
      via: "CommandApp with TypeRegistrar for DI"
      pattern: "CommandApp.*TypeRegistrar"
    - from: "Portless.IntegrationTests/ProxyProcessTests.cs"
      to: "Portless.Core.ProcessManagement.ProxyProcessManager"
      via: "Service collection DI"
      pattern: "Services\\.AddSingleton.*ProxyProcessManager"
    - from: "Portless.IntegrationTests/PortAllocatorTests.cs"
      to: "System.Net.Sockets.TcpListener"
      via: "Port availability checking"
      pattern: "TcpListener\\.Start.*IPAddress\\.Any"
---

<objective>
Create integration test project for CLI commands, proxy process management, and port allocation.

Purpose: Validate core components work correctly when integrated together without full E2E tool installation overhead.
Output: Integration test project with tests for CLI commands (TEST-01), process management (TEST-03), and port allocation (TEST-04 partial).
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-tests-automation-for-dotnet-tool/08-CONTEXT.md
@.planning/phases/08-integration-tests-automation-for-dotnet-tool/08-RESEARCH.md

# Reference existing test infrastructure
@Portless.Tests/Portless.Tests.csproj
@Portless.Tests/ProxyRoutingTests.cs

# Reference components under test
@Portless.Cli/Program.cs
@Portless.Core/ProcessManagement/ProxyProcessManager.cs
</context>

<tasks>

<task type="auto">
  <name>Create integration test project structure</name>
  <files>Portless.IntegrationTests/Portless.IntegrationTests.csproj</files>
  <action>
    Create Portless.IntegrationTests project with:
    - TargetFramework: net10.0
    - Package references: xUnit 2.9.3, Microsoft.NET.Test.Sdk 17.14.1, coverlet.collector 6.0.4
    - Package reference: Spectre.Console.Testing (latest stable) for CLI output capture
    - Project references: Portless.Cli, Portless.Core, Portless.Proxy
    - Using global: XUnit, Spectre.Console

    DO NOT add Microsoft.AspNetCore.Mvc.Testing (integration tests don't need TestServer)
    DO NOT add Moq (CONTEXT.md requires real components, not mocks)

    Add project to Portless.slnx after Portless.Tests
  </action>
  <verify>dotnet build Portless.IntegrationTests/Portless.IntegrationTests.csproj succeeds</verify>
  <done>Integration test project compiles with all references resolved</done>
</task>

<task type="auto">
  <name>Create CLI command integration tests</name>
  <files>Portless.IntegrationTests/CliCommandTests.cs</files>
  <action>
    Create CliCommandTests class with:
    - Test class inherits from IDisposable (or IAsyncLifetime if needed for cleanup)
    - Constructor creates ServiceCollection with AddPortlessPersistence, AddPortlessProcessManagement
    - Test: ListCommand_WithNoRoutes_DisplaysEmptyTable
      * Create CommandApp with TypeRegistrar from services
      * Configure app.AddCommand<ListCommand>("list")
      * Capture console output using StringWriter or Spectre.Console.Testing helpers
      * Run app.RunAsync(new[] { "list" })
      * Assert result equals 0 and output contains "No active routes"
    - Test: ListCommand_WithActiveRoutes_DisplaysTable
      * Create RouteStore and add test route (api1.localhost:4001)
      * Run list command
      * Assert output contains hostname and port
    - Test: RunCommand_WithMissingArgs_ShowsValidationError
      * Run with empty args
      * Assert non-zero exit code
      * Assert output contains "required" or "missing"

    Use CommandApp pattern from RESEARCH.md Example 1
    DO NOT use Process.Start (in-process testing per RESEARCH.md)
  </action>
  <verify>dotnet test --filter "FullyQualifiedName~CliCommandTests" passes all tests</verify>
  <done>CLI commands execute correctly with in-process testing</done>
</task>

<task type="auto">
  <name>Create proxy process lifecycle tests</name>
  <files>Portless.IntegrationTests/ProxyProcessTests.cs</files>
  <action>
    Create ProxyProcessTests class with:
    - Test class with IAsyncLifetime for per-test setup/teardown
    - Private field: string TestDirectory = Path.Combine(Path.GetTempPath(), Path.GetRandomFileName())
    - InitializeAsync: Create TestDirectory
    - DisposeAsync: Delete TestDirectory with retry logic (5 attempts, exponential backoff, log warnings on failure)

    Test: ProxyProcessManager_StartStop_CleansUpPidFile
      - Create ProxyProcessManager instance
      - Call StartProxyAsync with dotnet run --project Portless.Proxy/Portless.Proxy.csproj
      - Assert result.Success is true
      - Assert result.ProcessId has value
      - Wait 2 seconds for startup
      - Call StopProxyAsync
      - Assert success
      - Assert PID file does not exist
      - Verify process actually terminated (Process.GetProcessById throws or HasExited)

    Test: ProxyProcessManager_StartWhenAlreadyRunning_ReturnsError
      - Start proxy once
      - Try to start again (simulates second instance)
      - Assert second start fails (already running error)
      - Cleanup: Stop proxy

    Use real Process.Start (no mocks per CONTEXT.md)
    Use 2-second delays between start/stop operations
  </action>
  <verify>dotnet test --filter "FullyQualifiedName~ProxyProcessTests" passes all tests</verify>
  <done>Proxy lifecycle management works with PID tracking</done>
</task>

<task type="auto">
  <name>Create port allocation tests</name>
  <files>Portless.IntegrationTests/PortAllocatorTests.cs</files>
  <action>
    Create PortAllocatorTests class with:
    - Test: AllocatePort_ReturnsAvailablePortInRange
      * Create PortAllocator instance
      * Call AllocatePort(4000, 4999)
      * Assert port in range 4000-4999
      * Verify port actually free by creating TcpListener(IPAddress.Any, port)
      * Try listener.Start()
      * If succeeds → port was free, cleanup listener.Stop()
      * If fails → Assert.Fail("Port was not actually available")

    - Test: AllocatePort_MultipleTimes_ReturnsUniquePorts
      * Allocate 10 ports
      * Assert all unique
      * Verify all in range

    - Test: AllocatePort_ExhaustedRange_ThrowsException
      * Simulate exhaustion by testing very small range (4000-4001)
      * Try to allocate 3 times
      * Assert third call throws InvalidOperationException

    Use TcpListener for port availability verification
    DO NOT hard-code port numbers in tests
  </action>
  <verify>dotnet test --filter "FullyQualifiedName~PortAllocatorTests" passes all tests</verify>
  <done>Port allocation detects free ports and handles conflicts</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run dotnet test Portless.IntegrationTests/Portless.IntegrationTests.csproj — all tests pass
2. Verify test count: at least 7 tests (3 CLI + 2 proxy + 2 port allocation)
3. Run tests with --logger "console;verbosity=detailed" — see individual test results
4. Check no temp directories leaked after test run
</verification>

<success_criteria>
- Integration test project created and added to solution
- All CLI command tests pass (list, run validation)
- All proxy process tests pass (start/stop/cleanup)
- All port allocation tests pass (detection, uniqueness, exhaustion)
- Tests run independently (no shared state)
- No resource leaks (temp directories, processes) after test completion
</success_criteria>

<output>
After completion, create .planning/phases/08-integration-tests-automation-for-dotnet-tool/08-01-SUMMARY.md
</output>
