---
phase: 08-integration-tests-automation-for-dotnet-tool
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - Portless.Tests/YarpProxyIntegrationTests.cs
  - Portless.Tests/RoutePersistenceIntegrationTests.cs
  - README.md
autonomous: true
requirements:
  - TEST-02
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - YARP proxy routes requests correctly based on Host header
    - Route persistence works with file locking and hot-reload
    - Process management forwards signals and cleans up routes
    - All tests run on Windows and Linux
    - Documentation explains how to run tests
  artifacts:
    - path: "Portless.Tests/YarpProxyIntegrationTests.cs"
      provides: "YARP routing integration tests with WebApplicationFactory"
      contains: "IClassFixture<WebApplicationFactory<Program>>"
      min_lines: 100
    - path: "Portless.Tests/RoutePersistenceIntegrationTests.cs"
      provides: "Route persistence, file locking, and cleanup tests"
      contains: "RouteStore, Mutex, FileSystemWatcher"
      min_lines: 120
    - path: "README.md"
      provides: "Documentation for running tests"
      contains: "## Testing section with dotnet test commands"
  key_links:
    - from: "Portless.Tests/YarpProxyIntegrationTests.cs"
      to: "Portless.Proxy.Program"
      via: "WebApplicationFactory<Program> for in-memory TestServer"
      pattern: "WebApplicationFactory<Program>"
    - from: "Portless.Tests/RoutePersistenceIntegrationTests.cs"
      to: "Portless.Core.Configuration.RouteStore"
      via: "Direct instantiation for persistence testing"
      pattern: "RouteStore.*SaveRoutesAsync"
    - from: "README.md"
      to: "All test projects"
      via: "dotnet test commands"
      pattern: "dotnet test.*\\.csproj"
---

<objective>
Enhance existing test project with comprehensive YARP proxy and route persistence integration tests.

Purpose: Complete test coverage for proxy routing (TEST-02) and route persistence/cleanup (TEST-04) building on existing WebApplicationFactory patterns.
Output: Enhanced Portless.Tests with YARP integration tests and route persistence tests, plus README documentation.
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-tests-automation-for-dotnet-tool/08-CONTEXT.md
@.planning/phases/08-integration-tests-automation-for-dotnet-tool/08-RESEARCH.md

# Reference existing tests
@Portless.Tests/ProxyRoutingTests.cs
@Portless.Tests/RoutePersistenceTests.cs
@Portless.Tests/HotReloadTests.cs
@Portless.Tests/RouteCleanupTests.cs

# Reference components under test
@Portless.Core/Configuration/DynamicConfigProvider.cs
@Portless.Core/Configuration/RouteStore.cs
@Portless.Core/Services/RouteCleanupService.cs
</context>

<tasks>

<task type="auto">
  <name>Create comprehensive YARP proxy integration tests</name>
  <files>Portless.Tests/YarpProxyIntegrationTests.cs</files>
  <action>
    Create YarpProxyIntegrationTests class (extending or enhancing ProxyRoutingTests):
    - Test class: IClassFixture<WebApplicationFactory<Program>>
    - Constructor: Store factory, create HttpClient, get DynamicConfigProvider from services
    - Use WebApplicationFactory pattern from existing ProxyRoutingTests.cs

    Test: SingleHostname_RoutesToCorrectBackend
      - Create routes and clusters for api1.localhost → http://localhost:5000
      - Update config provider
      - Create HttpRequestMessage with Host header: api1.localhost
      - Send request via client
      - Assert status is OK/BadGateway/ServiceUnavailable/GatewayTimeout (any valid proxy response)

    Test: MultipleHostnames_RouteToDifferentBackends
      - Configure api1.localhost → port 5001, api2.localhost → port 5002
      - Send requests to both hostnames
      - Verify different clusters used (check response headers or status codes)

    Test: UnknownHostname_Returns404
      - Configure only api1.localhost
      - Send request to unknown.localhost
      - Assert status is 404 or similar not found status

    Test: ApiAddHostEndpoint_UpdatesRoutesDynamically
      - POST to /api/v1/add-host with {hostname, port}
      - Verify config provider updated
      - Send request with new hostname
      - Verify routing works

    Test: ProxyForwardsHeadersCorrectly
      - Send request with custom headers (X-Test, User-Agent)
      - Verify headers forwarded to backend (use test backend that echoes headers)

    If ProxyRoutingTests.cs already exists, either:
    a) Add new tests to existing class, or
    b) Create new YarpProxyIntegrationTests with additional test scenarios

    Use WebApplicationFactory for all tests (no real HTTP servers needed)
  </action>
  <verify>dotnet test --filter "FullyQualifiedName~YarpProxyIntegrationTests" passes all tests</verify>
  <done>YARP routing works correctly with multiple hostnames and dynamic updates</done>
</task>

<task type="auto">
  <name>Create route persistence and cleanup integration tests</name>
  <files>Portless.Tests/RoutePersistenceIntegrationTests.cs</files>
  <action>
    Create RoutePersistenceIntegrationTests class (extending or enhancing RoutePersistenceTests):
    - Test class with IAsyncLifetime
    - Private fields: string TestDirectory, RouteStore RouteStore
    - InitializeAsync: Create unique temp directory, create RouteStore with test directory
    - DisposeAsync: Delete test directory with retry logic

    Test: SaveRoutesAsync_PersistsToFile
      - Create test routes (api1.localhost:4001, api2.localhost:4002)
      - Call SaveRoutesAsync
      - Assert routes.json file exists
      - Read file, verify JSON structure matches RouteInfo model

    Test: LoadRoutesAsync_RestoresFromFile
      - Save routes
      - Create new RouteStore instance
      - Call LoadRoutesAsync
      - Assert routes match saved data

    Test: SaveRoutesAsync_WithConcurrentAccess_UsesFileLocking
      - Create two RouteStore instances with same directory
      - Start Task.Run for SaveAsync on instance1
      - Start Task.Run for SaveAsync on instance2
      - Await both tasks
      - Assert one succeeds, one waits for lock (verify no corruption)
      - Verify file contains valid JSON

    Test: RouteCleanupService_RemovesDeadProcesses
      - Save route with fake PID (e.g., 99999 - non-existent)
      - Create RouteCleanupService with test state directory
      - Trigger cleanup (await background service execution)
      - Call LoadRoutesAsync
      - Assert fake PID route removed

    Test: RouteFileWatcher_ReloadsOnFileChange
      - Start proxy with test routes file
      - Wait for FileSystemWatcher to initialize
      - Modify routes.json externally (add new route)
      - Wait 500ms for debounce
      - Send request to new hostname
      - Assert routing works (hot-reload successful)

    If RoutePersistenceTests.cs exists, add new tests or create comprehensive integration tests
    Use real file I/O (no mocks per CONTEXT.md)
  </action>
  <verify>dotnet test --filter "FullyQualifiedName~RoutePersistenceIntegrationTests" passes all tests</verify>
  <done>Route persistence, file locking, cleanup, and hot-reload work correctly</done>
</task>

<task type="auto">
  <name>Add testing documentation to README</name>
  <files>README.md</files>
  <action>
    Add ## Testing section to README.md with:
    - Overview: "Portless.NET has three test suites for comprehensive validation"
    - Subsections for each test project:
      * Unit Tests (Portless.Tests): "Component-level tests with WebApplicationFactory for YARP routing"
      * Integration Tests (Portless.IntegrationTests): "In-process CLI and process management tests"
      * E2E Tests (Portless.E2ETests): "Full tool installation and workflow tests"

    - Commands to run tests:
      ```bash
      # Run all tests
      dotnet test

      # Run specific test project
      dotnet test Portless.Tests/Portless.Tests.csproj
      dotnet test Portless.IntegrationTests/Portless.IntegrationTests.csproj
      dotnet test Portless.E2ETests/Portless.E2ETests.csproj

      # Run with coverage
      dotnet test --collect:"XPlat Code Coverage"

      # Run specific test
      dotnet test --filter "FullyQualifiedName~ProxyRoutingTests"
      ```

    - Cross-platform testing note: "Tests validated on Windows and Linux (Ubuntu/Debian)"
    - Test organization: "Each test is independent with unique temp directory, cleanup on completion"
    - Manual testing: "Run E2E tests before each release to validate tool installation"

    Add section after existing content, before "## Contributing" if present
  </action>
  <verify>README.md contains ## Testing section with dotnet test commands</verify>
  <done>Documentation explains how to run all test suites</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run dotnet test Portless.Tests/Portless.Tests.csproj — all tests pass
2. Verify YARP integration tests cover routing, dynamic updates, and headers
3. Verify persistence tests cover save/load, locking, cleanup, and hot-reload
4. Check README.md has testing section with commands
5. Run all tests from README commands — all pass
</verification>

<success_criteria>
- YARP integration tests cover multiple routing scenarios
- Route persistence tests cover save/load, locking, cleanup, and hot-reload
- All tests use WebApplicationFactory or real file I/O (no mocks)
- README.md documents testing approach and commands
- All tests in Portless.Tests pass
</success_criteria>

<output>
After completion, create .planning/phases/08-integration-tests-automation-for-dotnet-tool/08-03-SUMMARY.md
</output>
