---
phase: 05-process-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Portless.Core/Services/IProcessManager.cs
  - Portless.Core/Services/ProcessManager.cs
  - Portless.Core/Services/ProcessHealthMonitor.cs
  - Portless.Core/Extensions/ServiceCollectionExtensions.cs
  - Portless.Cli/Commands/RunCommand/RunCommand.cs
autonomous: true
requirements:
  - PROC-01
  - PROC-02
  - PROC-03

must_haves:
  truths:
    - "System spawns user command with PORT environment variable injected"
    - "System tracks process PID and stores it in RouteInfo"
    - "System monitors process health via polling every 5 seconds"
    - "System cleans up routes and ports when process terminates"
    - "System detects PID recycling to avoid false positives"
  artifacts:
    - path: "Portless.Core/Services/IProcessManager.cs"
      provides: "Process spawning and tracking interface"
      exports: ["StartManagedProcess", "GetProcessStatus"]
    - path: "Portless.Core/Services/ProcessManager.cs"
      provides: "Process spawning implementation with PORT injection"
      min_lines: 40
    - path: "Portless.Core/Services/ProcessHealthMonitor.cs"
      provides: "BackgroundService for process health polling"
      min_lines: 60
    - path: "Portless.Cli/Commands/RunCommand/RunCommand.cs"
      provides: "Run command using ProcessManager for spawning"
      contains: "StartManagedProcess"
  key_links:
    - from: "Portless.Cli/Commands/RunCommand/RunCommand.cs"
      to: "Portless.Core/Services/IProcessManager"
      via: "Dependency injection in constructor"
      pattern: "IProcessManager.*_processManager"
    - from: "Portless.Core/Services/ProcessHealthMonitor.cs"
      to: "Portless.Core/Services/IRouteStore"
      via: "LoadRoutesAsync in ExecuteAsync polling loop"
      pattern: "LoadRoutesAsync.*ExecuteAsync"
    - from: "Portless.Core/Services/ProcessHealthMonitor.cs"
      to: "Portless.Core/Services/IPortAllocator"
      via: "ReleasePortAsync call for dead processes"
      pattern: "ReleasePortAsync"
---

<objective>
Implement process spawning with PORT injection, PID tracking, and background health monitoring for automatic cleanup of terminated processes.

Purpose: Enable Portless to spawn and manage application processes with proper lifecycle tracking, ensuring routes and ports are cleaned up when processes terminate naturally or crash.

Output: Process management infrastructure (IProcessManager, ProcessManager, ProcessHealthMonitor) integrated with RunCommand and RouteCleanupService.
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-process-management/05-CONTEXT.md
@.planning/phases/05-process-management/05-RESEARCH.md

# Reference prior phase implementations
@.planning/phases/04-port-management/04-02-SUMMARY.md
@.planning/phases/03-cli-commands/03-03-SUMMARY.md
@Portless.Core/Models/RouteInfo.cs
@Portless.Core/Services/RouteCleanupService.cs
@Portless.Cli/Commands/RunCommand/RunCommand.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IProcessManager interface and ProcessManager implementation</name>
  <files>
    Portless.Core/Services/IProcessManager.cs
    Portless.Core/Services/ProcessManager.cs
  </files>
  <action>
    Create IProcessManager interface in Portless.Core/Services/:
    - Method: Process StartManagedProcess(string command, string args, int port, string workingDirectory)
    - Method: Task<ProcessStatus> GetProcessStatusAsync(int pid)
    - ProcessStatus record: (bool IsRunning, DateTime? StartTime, DateTime? ExitTime)

    Create ProcessManager implementation in Portless.Core/Services/:
    - Constructor accepts ILogger<ProcessManager>
    - StartManagedProcess implementation:
      * Create ProcessStartInfo with FileName=command, Arguments=args
      * Set UseShellExecute=false (required for env injection per CONTEXT.md decision)
      * Set RedirectStandardOutput=false, RedirectStandardError=false (inherit stdout/stderr per CONTEXT.md)
      * Set CreateNoWindow=true (background execution)
      * Set WorkingDirectory=workingDirectory (current directory of `portless run` per CONTEXT.md)
      * Inject PORT: startInfo.Environment["PORT"] = port.ToString()
      * Call Process.Start(startInfo) and return Process object
      * Throw InvalidOperationException if process is null
    - GetProcessStatusAsync implementation:
      * Try Process.GetProcessById(pid)
      * Return ProcessStatus with HasExited, StartTime, ExitTime
      * Catch ArgumentException (PID doesn't exist) and return IsRunning=false

    DO NOT implement process waiting or exit monitoring - that's ProcessHealthMonitor's responsibility.
    DO NOT use UseShellExecute=true - prevents environment variable injection.
  </action>
  <verify>
    dotnet build Portless.slnx succeeds with no compilation errors
    IProcessManager interface has StartManagedProcess method accepting (command, args, port, workingDirectory)
    ProcessManager.StartManagedProcess uses ProcessStartInfo with UseShellExecute=false and Environment["PORT"] injection
  </verify>
  <done>
    IProcessManager interface created with StartManagedProcess signature
    ProcessManager spawns processes with PORT variable injected via Environment dictionary
    Process.Start returns Process object with valid PID for tracking
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ProcessHealthMonitor BackgroundService for polling</name>
  <files>
    Portless.Core/Services/ProcessHealthMonitor.cs
  </files>
  <action>
    Create ProcessHealthMonitor class in Portless.Core/Services/:
    - Inherit from BackgroundService (Microsoft.Extensions.Hosting)
    - Constructor: IRouteStore, IPortAllocator, ILogger<ProcessHealthMonitor>
    - Private field: TimeSpan _pollingInterval = TimeSpan.FromSeconds(5) (per CONTEXT.md decision)
    - Override ExecuteAsync(CancellationToken stoppingToken):
      * Log "Process health monitor starting with 5s polling interval"
      * while (!stoppingToken.IsCancellationRequested):
        - await Task.Delay(_pollingInterval, stoppingToken)
        - await CheckProcessHealthAsync(stoppingToken)
      * Log "Process health monitor stopping"
    - Private async Task CheckProcessHealthAsync(CancellationToken cancellationToken):
      * var routes = await _routeStore.LoadRoutesAsync(cancellationToken)
      * var deadRoutes = routes.Where(r => !IsProcessAlive(r)).ToArray()
      * if (deadRoutes.Any()):
        - Log "Detected {Count} dead processes"
        - foreach (var route in deadRoutes):
          + await _portAllocator.ReleasePortAsync(route.Port)
          + Log "Released port {Port} from dead process {Pid}"
        - var aliveRoutes = routes.Except(deadRoutes).ToArray()
        - await _routeStore.SaveRoutesAsync(aliveRoutes, cancellationToken)
        - Log "Cleanup complete: {AliveCount} routes remaining"
    - Private static bool IsProcessAlive(RouteInfo route):
      * try { var process = Process.GetProcessById(route.Pid) }
      * catch (ArgumentException) { return false } // PID doesn't exist
      * if (process.HasExited) { return false; }
      * // PID recycling detection (per RESEARCH.md)
      * if (process.StartTime > route.CreatedAt + TimeSpan.FromSeconds(1)) { return false; }
      * route.LastSeen = DateTime.UtcNow;
      * return true;

    CRITICAL: Use 5-second polling interval per CONTEXT.md locked decision.
    CRITICAL: Detect PID recycling via StartTime comparison per RESEARCH.md pitfall #2.
    CRITICAL: Coordinated cleanup - release ports AND remove routes atomically.
  </action>
  <verify>
    dotnet build Portless.slnx succeeds
    ProcessHealthMonitor inherits from BackgroundService
    ExecuteAsync implements 5-second polling loop with Task.Delay
    IsProcessAlive checks HasExited and PID recycling via StartTime comparison
  </verify>
  <done>
    ProcessHealthMonitor polls routes every 5 seconds for process health
    Dead processes detected via HasExited and PID recycling validation
    Coordinated cleanup releases ports and removes routes atomically
    BackgroundService pattern enables proper DI integration and lifecycle management
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate ProcessManager and ProcessHealthMonitor with DI and RunCommand</name>
  <files>
    Portless.Core/Extensions/ServiceCollectionExtensions.cs
    Portless.Cli/Commands/RunCommand/RunCommand.cs
  </files>
  <action>
    Modify Portless.Core/Extensions/ServiceCollectionExtensions.cs:
    - In AddPortlessPersistence method, after IPortAllocator registration:
      * services.AddSingleton<IProcessManager, ProcessManager>()
      * services.AddHostedService<ProcessHealthMonitor>() // Register as BackgroundService
    - Add ILogger<ProcessManager> parameter to AddPortlessPersistence for ProcessManager DI

    Modify Portless.Cli/Commands/RunCommand/RunCommand.cs:
    - Add IProcessManager _processManager field
    - Add IProcessManager to constructor parameters
    - In ExecuteAsync, replace process spawning logic (lines 98-117):
      * Remove old ProcessStartInfo creation and Process.Start code
      * Replace with: var process = _processManager.StartManagedProcess(
          commandArgs[0],                    // command
          string.Join(" ", commandArgs.Skip(1)), // args
          port,                              // allocated port
          Directory.GetCurrentDirectory()    // working directory per CONTEXT.md
        );
      * Keep port update logic (lines 119-123) - still needed for PID tracking
      * Keep rest of flow (route registration, persistence, success message)
    - Remove "using System.Diagnostics;" from imports if no longer needed

    CRITICAL: ProcessHealthMonitor registered as AddHostedService for automatic background execution.
    CRITICAL: RunCommand uses IProcessManager.StartManagedProcess instead of Process.Start.
    CRITICAL: WorkingDirectory is Directory.GetCurrentDirectory() per CONTEXT.md decision.
  </action>
  <verify>
    dotnet build Portless.slnx succeeds
    ServiceCollectionExtensions registers IProcessManager as singleton
    ServiceCollectionExtensions registers ProcessHealthMonitor as hosted service
    RunCommand constructor accepts IProcessManager parameter
    RunCommand.ExecuteAsync calls _processManager.StartManagedProcess with correct parameters
  </verify>
  <done>
    IProcessManager and ProcessHealthMonitor registered in DI container
    RunCommand uses ProcessManager for spawning instead of Process.Start
    ProcessHealthMonitor runs as background service with 5-second polling
    Solution builds and RunCommand compiles with new process management flow
  </done>
</task>

</tasks>

<verification>
Build verification: `dotnet build Portless.slnx` succeeds with 0 errors
Integration verification: IProcessManager resolves in RunCommand constructor
Background service verification: ProcessHealthMonitor registered as AddHostedService
Process spawning verification: StartManagedProcess uses ProcessStartInfo with UseShellExecute=false
Health monitoring verification: 5-second polling interval implemented in ExecuteAsync
PID recycling detection: IsProcessAlive compares process.StartTime with route.CreatedAt + 1 second
Coordinated cleanup: Dead routes trigger both port release AND route removal in same transaction
</verification>

<success_criteria>
PROC-01 (Sistema spawnea comando con variable PORT): StartManagedProcess injects PORT via ProcessStartInfo.Environment
PROC-02 (Sistema trackea PID de proceso): Process.Id stored in RouteInfo.Pid, tracked by ProcessHealthMonitor
PROC-03 (Sistema limpia ruta cuando proceso termina): ProcessHealthMonitor polls every 5s, detects dead processes, removes routes
All requirements satisfied: Process spawning with PORT injection, PID tracking, and automatic route/port cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/05-process-management/05-01-SUMMARY.md` with:
- Implementation summary of IProcessManager, ProcessManager, ProcessHealthMonitor
- Process spawning flow with PORT injection
- Health monitoring with 5-second polling and PID recycling detection
- Coordinated cleanup of routes and ports
- Integration with RunCommand and DI container
- Deviations from plan (if any)
- Technical decisions (BackgroundService pattern, polling interval, PID recycling detection)
</output>
