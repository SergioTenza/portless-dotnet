---
phase: 05-process-management
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - Portless.Cli/Commands/RunCommand/RunCommand.cs
  - Portless.Cli/Commands/ProxyCommand/ProxyStopCommand.cs
  - Portless.Cli/Services/IProxyProcessManager.cs
  - Portless.Cli/Services/ProxyProcessManager.cs
autonomous: false
requirements:
  - PROC-04

must_haves:
  truths:
    - "Ctrl+C in CLI forwards SIGTERM to spawned process"
    - "Process has 10 seconds to shut down gracefully before force kill"
    - "Proxy stop with active processes prompts user to decide termination"
    - "Signal forwarding works cross-platform (Windows, macOS, Linux)"
  artifacts:
    - path: "Portless.Cli/Commands/RunCommand/RunCommand.cs"
      provides: "Signal forwarding in RunCommand with Ctrl+C handler"
      contains: "ApplicationStopping.Register"
      min_lines: 80
    - path: "Portless.Cli/Commands/ProxyCommand/ProxyStopCommand.cs"
      provides: "User prompt for terminating active processes"
      contains: "Are you sure"
    - path: "Portless.Cli/Services/IProxyProcessManager.cs"
      provides: "Enhanced proxy process management with child process tracking"
      exports: ["GetActiveManagedProcesses", "KillManagedProcesses"]
  key_links:
    - from: "Portless.Cli/Commands/RunCommand/RunCommand.cs"
      to: "System.Diagnostics.Process"
      via: "SIGTERM forwarding on ApplicationStopping"
      pattern: "CloseMainWindow.*WaitForExit.*10000"
    - from: "Portless.Cli/Commands/ProxyCommand/ProxyStopCommand.cs"
      to: "Portless.Cli/Services/IProxyProcessManager"
      via: "GetActiveManagedProcesses check before stopping proxy"
      pattern: "GetActiveManagedProcesses.*StopAsync"
---

<objective>
Implement signal forwarding (SIGTERM) to spawned processes and graceful shutdown coordination with user prompts when stopping the proxy with active processes.

Purpose: Ensure clean shutdown of applications spawned by Portless, allowing processes to handle termination signals gracefully and giving users control over whether to terminate active processes when stopping the proxy.

Output: Signal forwarding infrastructure in RunCommand with Ctrl+C handling, and enhanced ProxyStopCommand with user prompt for active process termination.
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-process-management/05-CONTEXT.md
@.planning/phases/05-process-management/05-RESEARCH.md
@.planning/phases/05-process-management/05-01-SUMMARY.md

# Reference implementations from prior work
@Portless.Cli/Commands/RunCommand/RunCommand.cs
@Portless.Cli/Commands/ProxyCommand/ProxyStopCommand.cs
@Portless.Cli/Services/ProxyProcessManager.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Ctrl+C signal forwarding in RunCommand</name>
  <files>
    Portless.Cli/Commands/RunCommand/RunCommand.cs
  </files>
  <action>
    Modify Portless.Cli/Commands/RunCommand/RunCommand.cs:

    Add field to store spawned process reference:
    - private Process _spawnedProcess;

    In ExecuteAsync, after starting process via _processManager.StartManagedProcess:
    - Store process reference: _spawnedProcess = process;

    Add signal forwarding setup after process start (before route registration):
    - Create cancellation token source for graceful shutdown: var cts = CancellationTokenSource.CreateLinkedTokenSource(cancellationToken);
    - Register ApplicationStopping handler:
      ```csharp
      using var _ = cts.Token.Register(() =>
      {
          if (_spawnedProcess != null && !_spawnedProcess.HasExited)
          {
              AnsiConsole.MarkupLine("[yellow]Forwarding SIGTERM to process {Pid}...[/]", _spawnedProcess.Id);
              ForwardSignalToProcess(_spawnedProcess);
          }
      });
      ```

    Add private void ForwardSignalToProcess(Process process):
    - Log: _logger.LogInformation("Forwarding SIGTERM to process {Pid}", process.Id);
    - Try graceful shutdown first (GUI-friendly, cross-platform):
      * process.CloseMainWindow(); // Sends WM_CLOSE on Windows, SIGTERM on Unix
      * bool exitedGracefully = process.WaitForExit(10000); // 10-second timeout per CONTEXT.md
      * if (exitedGracefully):
        - Log: _logger.LogInformation("Process {Pid} exited gracefully", process.Id);
        - return;
    - Force kill if timeout expired:
      * Log: _logger.LogWarning("Process {Pid} did not exit gracefully, forcing termination", process.Id);
      * process.Kill(entireProcessTree: true);
      * process.WaitForExit(); // Ensure cleanup completes
      * Log: _logger.LogInformation("Process {Pid} terminated forcefully", process.Id);

    CRITICAL: Only forward SIGTERM per CONTEXT.md locked decision (no SIGINT).
    CRITICAL: 10-second timeout before force kill per CONTEXT.md decision.
    CRITICAL: Use CloseMainWindow first for GUI-friendly shutdown, then Kill for force termination.
    CRITICAL: Handle exceptions for processes that terminate during signal forwarding.
  </action>
  <verify>
    dotnet build Portless.slnx succeeds
    RunCommand stores _spawnedProcess field reference
    ApplicationStopping.Register calls ForwardSignalToProcess with _spawnedProcess
    ForwardSignalToProcess implements CloseMainWindow -> WaitForExit(10000) -> Kill flow
  </verify>
  <done>
    Ctrl+C in CLI triggers ApplicationStopping callback
    SIGTERM forwarded to spawned process via CloseMainWindow
    10-second grace period for graceful shutdown before force kill
    Process terminates cleanly or forcefully after timeout
  </done>
</task>

<task type="auto">
  <name>Task 2: Track spawned processes in ProxyProcessManager</name>
  <files>
    Portless.Cli/Services/IProxyProcessManager.cs
    Portless.Cli/Services/ProxyProcessManager.cs
  </files>
  <action>
    Modify Portless.Cli/Services/IProxyProcessManager.cs:
    - Add method: Task<int[]> GetActiveManagedProcessesAsync() // Returns PIDs of processes spawned by portless run
    - Add method: Task KillManagedProcessesAsync(int[] pids) // Force kill specific processes

    Modify Portless.Cli/Services/ProxyProcessManager.cs:
    - Add private field: HashSet<int> _managedPids = new HashSet<int>();
    - Add private field: string _managedPidsFilePath; // Path to ~/.portless/managed-pids.json
    - In constructor: Initialize _managedPidsFilePath = Path.Combine(_stateDirectory, "managed-pids.json");
    - Create private async Task LoadManagedPidsAsync():
      * if (!File.Exists(_managedPidsFilePath)) return;
      * var json = await File.ReadAllTextAsync(_managedPidsFilePath);
      * _managedPids = JsonSerializer.Deserialize<HashSet<int>>(json) ?? new HashSet<int>();
    - Create private async Task SaveManagedPidsAsync():
      * var json = JsonSerializer.Serialize(_managedPids);
      * await File.WriteAllTextAsync(_managedPidsFilePath, json);
    - Implement GetActiveManagedProcessesAsync():
      * await LoadManagedPidsAsync();
      * Filter out dead PIDs:
        ```csharp
        var alivePids = _managedPids.Where(pid =>
        {
            try { var p = Process.GetProcessById(pid); return !p.HasExited; }
            catch (ArgumentException) { return false; }
        }).ToArray();
        return alivePids;
        ```
    - Implement KillManagedProcessesAsync(int[] pids):
      * foreach (var pid in pids):
        - try { var p = Process.GetProcessById(pid); p.Kill(entireProcessTree: true); }
        - catch (ArgumentException) { } // Process already dead
      * Remove from _managedPids
      * await SaveManagedPidsAsync();
    - Add public method: Task RegisterManagedProcessAsync(int pid) // Called by RunCommand after spawning
      * _managedPids.Add(pid);
      * await SaveManagedPidsAsync();

    DO NOT implement background monitoring - ProcessHealthMonitor already handles cleanup.
    DO store PIDs persistently for recovery after proxy restart.
  </action>
  <verify>
    dotnet build Portless.slnx succeeds
    IProxyProcessManager has GetActiveManagedProcessesAsync and KillManagedProcessesAsync methods
    ProxyProcessManager loads/saves _managedPids from JSON file
    GetActiveManagedProcessesAsync filters dead PIDs using Process.GetProcessById
    KillManagedProcessesAsync kills processes and updates JSON file
  </verify>
  <done>
    Managed process PIDs tracked in ~/.portless/managed-pids.json
    GetActiveManagedProcessesAsync returns array of active process PIDs
    KillManagedProcessesAsync force kills specified processes
    Persistent tracking survives proxy restarts
  </done>
</task>

<task type="auto">
  <name>Task 3: Add user prompt to ProxyStopCommand for active processes</name>
  <files>
    Portless.Cli/Commands/ProxyCommand/ProxyStopCommand.cs
  </files>
  <action>
    Modify Portless.Cli/Commands/ProxyCommand/ProxyStopCommand.cs:

    In ExecuteAsync, before calling _proxyManager.StopAsync():
    - Check for active managed processes: var activePids = await _proxyManager.GetActiveManagedProcessesAsync();
    - if (activePids.Length > 0):
      * Display warning: AnsiConsole.MarkupLine($"[yellow]Warning:[/] {activePids.Length} active process(es) spawned by Portless:");
      * List processes: foreach (var pid in activePids) { AnsiConsole.MarkupLine($"  - PID {pid}"); }
      * Prompt user: var confirm = AnsiConsole.Confirm("Stop these processes along with the proxy?", defaultValue: false);
      * if (!confirm):
        - AnsiConsole.MarkupLine("[yellow]Proxy stop cancelled.[/]");
        - AnsiConsole.MarkupLine("Processes will continue running. Use [yellow]'portless list'[/] to see active routes.");
        - return 0;
      * else:
        - AnsiConsole.MarkupLine("[yellow]Stopping processes...[/]");
        - await _proxyManager.KillManagedProcessesAsync(activePids);
        - AnsiConsole.MarkupLine("[green]✓[/] Processes stopped");

    - Continue with normal proxy stop flow: await _proxyManager.StopAsync();
    - Show success message: AnsiConsole.MarkupLine("[green]✓[/] Proxy stopped");

    CRITICAL: Prompt user BEFORE stopping proxy per CONTEXT.md decision.
    CRITICAL: Default to false (don't kill) to avoid accidental data loss.
    CRITICAL: Show clear list of affected PIDs so user understands impact.
    CRITICAL: Allow cancellation if user doesn't want to terminate processes.

    Error handling:
    - Catch exceptions during process killing and show warnings but continue stopping proxy
  </action>
  <verify>
    dotnet build Portless.slnx succeeds
    ProxyStopCommand calls GetActiveManagedProcessesAsync before StopAsync
    Active PIDs displayed to user in list format
    AnsiConsole.Confirm prompt shown with default=false
    KillManagedProcessesAsync called only if user confirms
  </verify>
  <done>
    ProxyStopCommand checks for active processes before stopping
    User prompted with list of PIDs and confirm prompt
    Processes killed only if user confirms (default=false)
    Proxy stop cancelled if user declines to kill processes
    Clear messaging about process termination
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete signal forwarding and graceful shutdown infrastructure</what-built>
  <how-to-verify>
    1. Start proxy: `dotnet run --project Portless.Cli -- proxy start`
    2. Run test process: `dotnet run --project Portless.Cli -- run testapi dotnet run --project TestApi`
    3. Verify process starts with "Running on http://testapi.localhost" message
    4. Press Ctrl+C to trigger graceful shutdown
    5. Verify "Forwarding SIGTERM to process XXX" message appears
    6. Verify process exits cleanly (check with `portless list` - should be empty)
    7. Test proxy stop with active processes:
       - Run another process: `dotnet run --project Portless.Cli -- run testapi2 dotnet run --project TestApi`
       - Try to stop proxy: `dotnet run --project Portless.Cli -- proxy stop`
       - Verify prompt appears with active PIDs listed
       - Confirm termination (or cancel to test)
    8. If confirmed: Verify processes killed and proxy stops
  </how-to-verify>
  <resume-signal>Type "approved" if signal forwarding and graceful shutdown work correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Build verification: `dotnet build Portless.slnx` succeeds with 0 errors
Signal forwarding verification: RunCommand has ApplicationStopping.Register callback calling ForwardSignalToProcess
Graceful shutdown verification: ForwardSignalToProcess uses CloseMainWindow -> WaitForExit(10000) -> Kill flow
Process tracking verification: ProxyProcessManager saves _managedPids to JSON file
User prompt verification: ProxyStopCommand shows active PIDs and confirms before killing
Cross-platform verification: CloseMainWindow works on Windows/macOS/Linux (per RESEARCH.md)
Timeout verification: 10-second grace period implemented in WaitForExit call
</verification>

<success_criteria>
PROC-04 (Sistema forwarda signals SIGTERM/SIGINT): RunCommand forwards SIGTERM via CloseMainWindow on Ctrl+C
Graceful shutdown: 10-second timeout before force kill implemented
User control: ProxyStopCommand prompts user before terminating active processes
Cross-platform: CloseMainWindow pattern works on all supported platforms (Windows, macOS, Linux)
All requirements satisfied: Signal forwarding with graceful shutdown coordination
</success_criteria>

<output>
After completion, create `.planning/phases/05-process-management/05-02-SUMMARY.md` with:
- Implementation summary of signal forwarding in RunCommand
- Graceful shutdown flow with 10-second timeout
- Process tracking in ProxyProcessManager with persistent storage
- User prompt implementation in ProxyStopCommand
- Cross-platform signal handling approach
- Test results from human verification checkpoint
- Deviations from plan (if any)
- Technical decisions (CloseMainWindow vs Kill, 10-second timeout, default=false for prompt)
</output>
