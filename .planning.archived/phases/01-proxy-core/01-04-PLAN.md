---
phase: 01-proxy-core
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Portless.Tests/ProxyRoutingTests.cs
autonomous: true
gap_closure: true
requirements:
  - PROXY-02
  - PROXY-03
  - PROXY-04

must_haves:
  truths:
    - "Automated tests verify Host-based routing behavior"
    - "Tests cover single hostname, multiple hostnames, and invalid hostname scenarios"
    - "Tests verify configuration update triggers routing changes"
  artifacts:
    - path: "Portless.Tests/ProxyRoutingTests.cs"
      provides: "Integration tests for YARP routing"
      min_lines: 50
      contains:
        - '"Fact"'
        - '"HttpClient"'
        - '"TestServer"'
        - '"WebApplicationFactory"'
  key_links:
    - from: "ProxyRoutingTests"
      to: "Portless.Proxy/Program.cs"
      via: "WebApplicationFactory<TestEntrypoint> or TestServer"
      pattern: "WebApplicationFactory.*TestServer"
    - from: "ProxyRoutingTests"
      to: "YARP routing"
      via: "HttpClient requests with Host headers"
      pattern: "client.DefaultRequestHeaders.Add.*Host"

---

<objective>
Create automated integration tests for YARP-based proxy routing to verify Host header routing behavior.

Purpose: Plan 01-02 specified ProxyRoutingTests.cs with minimum 50 lines containing "Fact", "HttpClient", and "TestServer" patterns, but the file was never created. This gap blocks verification that routing works correctly.

Output: Integration test file with Fact tests covering single hostname routing, multiple hostnames routing, invalid hostnames, and configuration updates.
</objective>

<execution_context>
@C:/Users/serge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-proxy-core/01-01-PLAN.md
@.planning/phases/01-proxy-core/01-02-PLAN.md
@.planning/phases/01-proxy-core/01-02-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-proxy-core/01-RESEARCH.md

# Gap Source
@.planning/phases/01-proxy-core/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProxyRoutingTests.cs with integration tests</name>
  <files>Portless.Tests/ProxyRoutingTests.cs</files>
  <action>
    Create Portless.Tests/ProxyRoutingTests.cs with integration tests for YARP routing functionality.

    **File requirements (from Plan 01-02 spec):**
    - Minimum 50 lines
    - Contains "Fact" attribute (xUnit test marker)
    - Uses HttpClient for making test requests
    - Uses TestServer or WebApplicationFactory<TestEntrypoint> for in-memory test server

    **Test scenarios to implement:**

    1. **Single hostname routes to correct backend:**
       - Create test server with Portless.Proxy
       - Add route for "api1.localhost" → "http://localhost:5000"
       - Make request with Host: api1.localhost
       - Verify request forwards to correct backend

    2. **Multiple hostnames route to different backends:**
       - Add route for "api1.localhost" → "http://localhost:5000"
       - Add route for "web1.localhost" → "http://localhost:3000"
       - Verify each hostname routes to correct backend port

    3. **Invalid hostnames return 404:**
       - Make request with Host: unknown.localhost
       - Verify 404 response (no matching route)

    4. **Configuration update triggers routing changes:**
       - Add initial route
       - Verify routing works
       - Add second route via API
       - Verify both routes work (config reloaded)

    **Implementation guidance:**
    - Use WebApplicationFactory<TestEntrypoint> if Portless.Proxy has Program.cs with public TestEntrypoint
    - Otherwise use TestServer with WebHostBuilder configured for Portless.Proxy
    - Set Host header via: `client.DefaultRequestHeaders.Add("Host", "api1.localhost")`
    - Use HttpClient to make requests: `await client.GetAsync("/")`
    - Verify response status codes and content
    - Mock backend servers or use in-memory test servers for backends

    **Reference pattern from RESEARCH.md:**
    ```csharp
    public class ProxyRoutingTests : IClassFixture<WebApplicationFactory<TestEntrypoint>>
    {
        private readonly HttpClient _client;
        private readonly WebApplicationFactory<TestEntrypoint> _factory;

        public ProxyRoutingTests(WebApplicationFactory<TestEntrypoint> factory)
        {
            _factory = factory;
            _client = factory.CreateClient();
        }

        [Fact]
        public async Task SingleHostname_RoutesToCorrectBackend()
        {
            // Arrange: Add route via API
            // Act: Make request with Host header
            // Assert: Verify routing
        }
    }
    ```

    **Key imports needed:**
    - `using Xunit;` (for [Fact] attribute)
    - `using Microsoft.AspNetCore.TestHost;` (for TestServer)
    - `using Microsoft.AspNetCore.Mvc.Testing;` (for WebApplicationFactory)
    - `using System.Net.Http;` (for HttpClient)

    **Note:** Portless.Tests project already references xUnit 2.9.3 (from REQUIREMENTS.md stack).
  </action>
  <verify>
    1. Build: `dotnet build Portless.slnx`
    2. Run tests: `dotnet test Portless.Tests/Portless.Tests.csproj --filter "FullyQualifiedName~ProxyRoutingTests"`
    3. Verify file exists at Portless.Tests/ProxyRoutingTests.cs
    4. Verify file contains "Fact", "HttpClient", "TestServer" or "WebApplicationFactory"
    5. Verify minimum 50 lines
    6. Verify tests compile without errors
  </verify>
  <done>
    ProxyRoutingTests.cs exists with 4+ Fact tests covering single hostname routing, multiple hostnames routing, invalid hostname handling, and configuration update scenarios. Tests use HttpClient with TestServer or WebApplicationFactory for in-memory testing.
  </done>
</task>

</tasks>

<verification>
After creation, verify:
- File contains at least 4 [Fact] tests
- Tests use HttpClient for making requests
- Tests set Host header for routing
- Tests verify response status codes
- Tests compile and can run (may fail if backend servers not mocked - that's OK for structure verification)
</verification>

<success_criteria>
- [ ] Portless.Tests/ProxyRoutingTests.cs created with minimum 50 lines
- [ ] File contains "Fact" attribute (xUnit test marker)
- [ ] File uses HttpClient for making requests
- [ ] File uses TestServer or WebApplicationFactory for in-memory hosting
- [ ] Tests cover single hostname, multiple hostnames, invalid hostname, config update scenarios
- [ ] Solution builds successfully with tests included
</success_criteria>

<output>
After completion, create `.planning/phases/01-proxy-core/01-04-SUMMARY.md`
</output>
